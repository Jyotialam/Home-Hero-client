import React, { useEffect } from 'react';

const Services = () => {
      useEffect(() => {
        document.title = "Register | Home-hero";
      }, []);
    return (
        <div>
            All Services
        </div>
    );
};

export default Services;